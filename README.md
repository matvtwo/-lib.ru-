Проект представляет собой серверное веб-приложение, реализующее интеллектуальный HTTP-прокси для исторического литературного архива lib.ru с последующей трансформацией и адаптацией гипертекстового контента под современные пользовательские интерфейсы чтения.

В рамках проекта реализован слой промежуточной обработки HTTP-запросов, который динамически маршрутизирует обращения к различным доменным зонам экосистемы lib.ru (lib.ru, lit.lib.ru, az.lib.ru), корректно разрешая относительные и абсолютные ссылки, восстанавливая иерархию каталогов и предотвращая уход пользователя на внешние ресурсы.

Особое внимание уделено обработке и нормализации текстовых данных, представленных в устаревших кодировках (Windows-1251, KOI8-R), с последующим приведением их к унифицированному UTF-8 представлению. Это обеспечивает корректную визуализацию контента в современных браузерах и упрощает дальнейшую работу с текстом.

Для страниц литературных произведений реализован специализированный режим «читалки», в котором HTML-структура оригинального документа преобразуется в поток чистого текста с сохранением логической структуры, удобной навигации, пагинации и пользовательского управления чтением.

Для каталогов авторов реализована клиентская интерактивная фильтрация, позволяющая выполнять локальный поиск по именам авторов без обращения к серверу, на основе анализа DOM-структуры страницы.

Архитектура проекта демонстрирует практическое применение принципов:
– HTTP-прокси и reverse-proxy,
– серверной трансформации HTML,
– работы с различными кодировками текста,
– DOM-манипуляций на стороне клиента,
– адаптации легаси-контента под современные UX-паттерны.

Проект может рассматриваться как пример интеграции исторических цифровых архивов в современные веб-системы чтения и анализа текста.

Используемые технологии:

Node.js (серверная платформа)

Express.js (HTTP-сервер и middleware-архитектура)

Fetch API (серверные HTTP-запросы)

iconv-lite (декодирование устаревших текстовых кодировок)

HTML string processing (серверная модификация гипертекста)

Vanilla JavaScript (клиентская фильтрация и интерактивность)

CSS (адаптивное оформление и пользовательская читалка)

более длительное описание 
# Интеллектуальный HTTP-прокси для литературного архива Lib.Ru  
### Серверная трансформация и адаптация легаси-контента для современных интерфейсов чтения

---

## 📌 Описание проекта

Проект представляет собой **серверное веб-приложение**, реализующее интеллектуальный **HTTP-прокси** для исторического литературного архива **lib.ru** с последующей **трансформацией и адаптацией гипертекстового контента** под современные пользовательские интерфейсы чтения.

Основная цель проекта — продемонстрировать, каким образом легаси-веб-ресурсы, созданные в рамках раннего этапа развития интернета, могут быть интегрированы в современные веб-системы без изменения исходного источника данных.

---

## 🧠 Архитектурные особенности

В рамках проекта реализован **промежуточный серверный слой обработки HTTP-запросов**, выполняющий следующие функции:

- динамическая маршрутизация запросов к различным доменным зонам экосистемы Lib.Ru  
  (`lib.ru`, `lit.lib.ru`, `az.lib.ru`);
- корректное разрешение **относительных и абсолютных ссылок**;
- восстановление и сохранение иерархии каталогов;
- предотвращение ухода пользователя на внешние сайты за пределы прокси-интерфейса.

Таким образом, пользователь взаимодействует исключительно с локальным сервером, в то время как оригинальный архив используется как источник данных.

---

## 🔤 Работа с текстовыми кодировками

Особое внимание уделено обработке и нормализации текстовых данных, представленных в устаревших кодировках:

- `Windows-1251`
- `KOI8-R`

Сервер автоматически определяет кодировку ответа и выполняет **декодирование с последующим приведением текста к UTF-8**, что обеспечивает:

- корректную визуализацию кириллического текста в современных браузерах;
- унифицированную обработку контента на сервере и клиенте;
- возможность дальнейшего анализа и трансформации текста.

---

## 📖 Режим чтения («читалка»)

Для страниц литературных произведений реализован специализированный **режим чтения**, в котором:

- исходная HTML-структура документа анализируется на сервере;
- содержимое преобразуется в поток **чистого текста**;
- удаляются визуальные артефакты и устаревшая разметка;
- сохраняется логическая структура произведения.

Интерфейс чтения поддерживает:

- вертикальную пагинацию;
- навигацию с клавиатуры;
- управление прокруткой;
- минималистичный адаптивный дизайн.

---

## 🔎 Интерактивная работа с каталогами

Для каталогов авторов реализована **клиентская интерактивная фильтрация**, позволяющая:

- выполнять локальный поиск по именам авторов;
- фильтровать элементы без дополнительных HTTP-запросов;
- работать непосредственно с DOM-структурой страницы.

Поиск осуществляется полностью на стороне клиента и не требует серверного взаимодействия.

---

## 🏗 Используемые принципы и подходы

Архитектура проекта демонстрирует практическое применение следующих концепций:

- HTTP-proxy и reverse-proxy;
- серверная трансформация HTML-документов;
- работа с устаревшими текстовыми кодировками;
- обработка легаси-контента;
- DOM-манипуляции на стороне клиента;
- адаптация исторических веб-ресурсов под современные UX-паттерны.

---

## 🧪 Применение и ценность проекта

Проект может рассматриваться как:

- учебный пример серверной обработки веб-контента;
- демонстрация работы с цифровыми архивами;
- эксперимент по модернизации легаси-ресурсов;
- основа для систем чтения, анализа и визуализации текстов.

---

## 🛠 Используемые технологии

### Серверная часть
- **Node.js** — серверная платформа выполнения JavaScript  
- **Express.js** — HTTP-сервер и middleware-архитектура  
- **Fetch API** — выполнение серверных HTTP-запросов  
- **iconv-lite** — декодирование устаревших текстовых кодировок  
- **HTML string processing** — серверная модификация гипертекста  

### Клиентская часть
- **Vanilla JavaScript** — клиентская интерактивность и фильтрация  
- **CSS** — адаптивное оформление интерфейсов и режима чтения  

---

## 📂 Назначение репозитория

Репозиторий предназначен для демонстрации **практических навыков серверного веб-программирования**, работы с текстовыми данными и архитектурного мышления при интеграции легаси-систем в современные веб-решения.


Vanilla JavaScript — интерактивность и DOM-фильтрация

CSS — адаптивное оформление и пользовательская читалка
